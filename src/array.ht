#ifndef ARRAY_H
#define ARRAY_H

%INCLUDE%

%TEMPLATE_BEGIN%
typedef struct {
    %TYPE%* items;
    int length;
    int capacity;
} Array_%TYPE%;

Array_%TYPE%* newArray_%TYPE%();
void freeArray_%TYPE%(Array_%TYPE%* a);
%TEMPLATE_END%

#endif // ARRAY_H
